apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "run-dsp.fullname" . }}-test-connection"
  labels:
    {{- include "run-dsp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "run-dsp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
